apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: "{{ .Values.namespace }}"
spec:
  selector:
    app: frontend-deployment
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
